# Operations Scripts

This folder is reserved for operations-level helper scripts or notes.

Channel-specific scripts should remain in channel folders such as:

- `scripts/ebay/`
- `scripts/etsy/`
- `scripts/catalog/`
- `scripts/inventory/`

Use this folder only when a script spans business operations across channels or produces `.agents/operations/reports/` artifacts.